# ebolasim-tools patch: 0003-std-max-compat
# Summary: Use std::max for a C++ Linux compatibility build.
# Target files: SpatialSim.c
# Model logic change: no
# Compile only: true
--- a/SpatialSim.c
+++ b/SpatialSim.c
@@ -11478,7 +11478,7 @@
 	if (P.UpdateVaccDosePerDay)
 	{
 		weeklyDC = 0;
-		for (i = max(n - 6,0); i <= n; i++)
+		for (i = std::max(n - 6, 0); i <= n; i++)
 		{
 			weeklyDC += TimeSeries[i].incDC;
 		}
